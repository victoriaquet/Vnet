@model IEnumerable<SNC2017.Models.CatalogoPremio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaCarga)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaBaja)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaCarga)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaBaja)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCatalogoPremio }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCatalogoPremio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCatalogoPremio })
        </td>
    </tr>
}

</table>
