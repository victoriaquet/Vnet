@model SNC2017.Models.Distribuidor

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Distribuidor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Codigo)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Codigo)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nombre)
        </dd>

    </dl>
</div>
<p>
    @if (User.IsInRole("Administrador"))
    {
        @Html.ActionLink("Crear Usuario", "Register", "Account", new { identificador = Model.IdDistribuidor.ToString(), role = "Distribuidor" }, new { @class = "btn btn-primary" });

        @Html.ActionLink("Editar", "Edit", new { id = Model.IdDistribuidor }, new { @class = "btn btn-primary" });
    };

    @Html.ActionLink("Back to List", "Index")
</p>
