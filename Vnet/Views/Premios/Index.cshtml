@model IEnumerable<SNC2017.Models.Premio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CatalogoPremio.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Suscriptor.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaPremio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CatalogoPremio.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Suscriptor.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaPremio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPremio }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPremio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPremio })
        </td>
    </tr>
}

</table>
